{
  "name": "afonso-chatbot-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "embed": "node scripts/embed.mjs",
    "check:embeddings": "node scripts/check-embeddings.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
